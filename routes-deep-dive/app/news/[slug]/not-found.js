export default function NewsSlugNotFoundPage() {
    return (
        <div id="error">
            <h1>404 - Article Not Found</h1>
            <p>Unfortunately, we could not find the requested article.</p>
        </div>
    );
}
